import React from 'react';

const Character = (props) => {
  // do stuff and things here
  if (props.tab === 0) {
    return (<h1>Character Page Under Construction</h1>)
  } else {
    return (<div></div>);
  }
}

export default Character;